# Kiến trúc

Nội dung tri thức nằm trong `knowledge/`; schema trong `schemas/`; engine Python
nằm trong `src/nhan_thuat/`. Các adapter API, database và UI về sau chỉ tiêu thụ
engine, không trở thành nơi lưu nội dung gốc.

