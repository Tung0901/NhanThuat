services:
  - type: web
    name: nhan-thuat-web
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: PYTHONPATH=src python -m backend.app.main
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.0"
      - key: PORT
        value: "8000"
